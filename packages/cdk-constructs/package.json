{
  "name": "@ocdk/cdk-constructs",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc -p tsconfig.build.json",
    "clean:light": "rm -rf ./dist && rm -rf ./tsconfig.build.tsbuildinfo",
    "clean:full": "yarn run clean:light && rm -rf ./node_modules"
  },
  "dependencies": {
    "@aws-cdk/aws-dynamodb": "^1.91.0",
    "@aws-cdk/aws-iam": "^1.91.0",
    "@aws-cdk/aws-lambda": "^1.91.0",
    "@aws-cdk/aws-stepfunctions": "^1.91.0",
    "@aws-cdk/aws-stepfunctions-tasks": "^1.91.0",
    "@aws-cdk/core": "^1.91.0",
    "@ocdk/function-dynamodb-stream-processor": "^1.0.0"
  }
}
